[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "snobol4c"
version = "0.2.0"
description = "SNOBOL4 pattern match engine — CPython C extension"
requires-python = ">=3.8"

[tool.setuptools]
py-modules = []
